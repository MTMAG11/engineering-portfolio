Week of 9/7 - 9/13:

September 7:
 - Continued development of the autonomous rocket flight simulator.
 - Reviewed the current simulator state and planned the next development stage.
 - Identified flight telemetry and summary metrics as the next priority, including burnout conditions and time to apogee.
 - Decided to finish the basic simulation/telemetry foundation before adding drag or moving into more advanced GNC work.

September 8:
 - Continued development and cleanup of the autonomous rocket flight simulator.
 - Refactored simulation.py so the simulation runs through a run_simulation() function and returns the flight data as simulation_results.
 - Added a proper if __name__ == "__main__": entry point so the simulation can later be imported by the UI without automatically running.
 - Verified that the refactored simulator still runs correctly and produces the expected graphs.
 - Removed unnecessary terminal output from the simulation workflow, moving toward having the future UI handle flight data and visualization.
 - Planned to complete the remaining simulator architecture cleanup alongside the UI development.
 - Planned the next major stage as building the first PySide6 UI with embedded Matplotlib graphs, flight telemetry, and a flight summary.